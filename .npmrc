# Expo / React Native and Next.js are happiest with a hoisted node_modules
# layout. This keeps the pnpm monorepo compatible with Metro and Next bundlers.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
